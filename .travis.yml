language: python
python:
  - "2.6"
  - "2.7"
  - "3.3"
  - "3.4"
# command to install dependencies
install:
  - if [[ $TRAVIS_PYTHON_VERSION == 2* ]]; then pip install future mock; fi
  - pip install -e .
# command to run tests
script:
  - python setup.py test --pytest-args=tests
