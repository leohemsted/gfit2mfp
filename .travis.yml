language: python
python:
  - "3.3"
  - "3.4"
install:
  - pip install -e .
  - pip install coveralls
script:
  - python setup.py test -a "--cov gfit2mfp --rcfile=setup.cfg"
after_success:
    coveralls
